create table queue_buffer
(
    id      BIGSERIAL primary key,
    payload TEXT not null
);

create table queue_buffer_1
(
    id      BIGSERIAL primary key,
    payload TEXT not null
);

create table queue_buffer_2
(
    id      BIGSERIAL primary key,
    payload TEXT not null
);

create table queue_buffer_3
(
    id      BIGSERIAL primary key,
    payload TEXT not null
);
